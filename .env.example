# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# ===========================================
# DATABASE
# ===========================================
# URL de conexão com o banco de dados
# Desenvolvimento (SQLite): file:./dev.db
# Produção (PostgreSQL): postgresql://user:password@host:5432/database
DATABASE_URL=postgresql://bankme:bankme_password@localhost:5432/bankme_db

# ===========================================
# APPLICATION
# ===========================================
# Ambiente de execução (development, production, test)
NODE_ENV=development

# Porta onde a aplicação irá rodar
PORT=3000

# ===========================================
# JWT (Authentication)
# ===========================================
# Secret para assinar tokens JWT (use uma string aleatória e segura)
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_PASS=your-super-secret-jwt-key-change-this

# Tempo de expiração do token (ex: 1d, 7d, 24h)
JWT_EXPIRATION=1d

# ===========================================
# REDIS (Queue/Cache)
# ===========================================
# Host do servidor Redis
REDIS_HOST=localhost

# Porta do servidor Redis
REDIS_PORT=6379